# 学生端考试系统 - 自动评分使用说明

## 概述

学生端客户端 Agent 支持**自动评分模式**，考试流程如下：

```
认证 → 获取题目 → 显示题目 → 自动评分 → 上传成绩 → 结束考试
```

## 快速开始

### 方式 1：自动评分模式（推荐）

```bash
cd /root/linux_exam_system/client_agent
python3 exam_agent.py --auto --server http://<服务器 IP>:3000
```

**特点**：
- 无需手动按 Enter 确认
- 显示题目后 5 秒自动开始评分
- 评分完成后自动上传成绩
- 自动结束考试

### 方式 2：手动确认模式

```bash
cd /root/linux_exam_system/client_agent
python3 exam_agent.py --server http://<服务器 IP>:3000
```

**特点**：
- 显示题目后等待学生按 Enter 键
- 适合需要时间完成操作考试的场景

## 完整流程说明

### Step 1: 身份认证

客户端自动使用当前系统用户名作为学生 ID 进行认证。

```
[认证] 正在向服务端认证 (http://localhost:3000)...
[认证] 认证成功！欢迎，student01
```

**认证逻辑**：
- 首次认证：绑定设备，获取 Token（8 小时有效）
- 后续认证：检查设备匹配，使用缓存 Token

### Step 2: 等待考试

客户端每 10 秒检查一次考试状态，最长等待 1 小时。

```
[等待] 正在等待考试开始...
[等待] 考试尚未开始.... (10s)
[等待] 考试尚未开始..... (20s)
```

### Step 3: 获取题目

认证成功后，自动从服务端获取考试题目和评分脚本。

```
[抽题] 正在从服务端获取题目...
[OK]    获取到 9 道题目
    题目 1: 数据库卸载 (4 分)
    题目 2: 重新安装部署数据库 (14 分)
    ...
```

### Step 4: 显示题目

在终端显示所有题目内容。

```
════════════════════════════════════════════
                    考试题目
════════════════════════════════════════════

【第 1 题】数据库卸载  (4 分)
----------------------------------------
请完成以下操作：
1. 停止数据库服务
2. 卸载数据库软件
3. 清理配置文件
...

【第 2 题】重新安装部署数据库  (14 分)
----------------------------------------
...
```

### Step 5: 自动评分

**自动模式**：等待 5 秒后自动开始评分

```
[自动模式] 将在 5 秒后开始自动评分...
[自动模式] 开始执行评分...

[评分] 开始执行评分脚本...

[评分] 正在评分第 1 题：数据库卸载...
       得分：4 / 4

[评分] 正在评分第 2 题：重新安装部署数据库...
       得分：12 / 14
       ✗ 数据库名错误 (-1 分)
       ✗ 实例名错误 (-1 分)
...
```

### Step 6: 上传成绩

评分完成后立即上传到服务端。

```
[上传] 正在上传成绩到服务端...
[完成] 成绩已成功上传！
       最终得分：85 / 100
       提交时间：2026-03-22 14:30:00
```

### Step 7: 结束考试

自动更新考试记录状态为已完成。

```
[结束] 正在结束考试...
[完成] 考试已结束，记录 ID: 123
```

## 命令行参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--auto` | 自动评分模式 | `python3 exam_agent.py --auto` |
| `--server` | 指定服务器地址 | `--server http://192.168.1.100:3000` |
| `--config` | 配置模式 | `--config` |
| `--test-connection` | 测试连接 | `--test-connection` |
| `--help` | 显示帮助 | `--help` |

### 配置服务器地址

```bash
python3 exam_agent.py --config
# 输入服务器地址，如：http://192.168.1.100:3000
```

配置文件保存在：`~/.exam_agent/config.json`

## 输出日志

### 正常流程日志

```
╔══════════════════════════════════════════════════════════╗
║           Linux 考试系统 - 客户端 Agent v1.2              ║
║           支持 score.sh 评分格式                          ║
║           模式：自动评分模式                        ║
╚══════════════════════════════════════════════════════════╝

[系统信息]
  当前用户：student01
  主机名：  exam-client-01
  操作系统：Linux 5.10.0-18-generic
  设备 ID:  a79bced6c52b9add...

[认证] 正在向服务端认证 (http://localhost:3000)...
[认证] 认证成功！欢迎，student01

[等待] 正在等待考试开始...
[考试] 发现考试：达梦数据库操作考试
       时长：120 分钟
       题目数：9 道

[抽题] 正在从服务端获取题目...
[OK]    获取到 9 道题目
    题目 1: 数据库卸载 (4 分)
    题目 2: 重新安装部署数据库 (14 分)
    题目 3: 表空间及用户规划 (8 分)
    题目 4: 表管理及数据导出 (18 分)
    题目 5: 创建视图 (8 分)
    题目 6: 数据库开发 (20 分)
    题目 7: 定时作业 (8 分)
    题目 8: 性能优化 (10 分)
    题目 9: 数据库安全 (10 分)

════════════════════════════════════════════
                    考试题目
════════════════════════════════════════════
...

[自动模式] 将在 5 秒后开始自动评分...

[评分] 开始执行评分脚本...
...

[上传] 正在上传成绩到服务端...
[完成] 成绩已成功上传！
       最终得分：85 / 100
       提交时间：2026-03-22 14:30:00

[结束] 正在结束考试...
[完成] 考试已结束，记录 ID: 123
```

### 错误处理日志

**认证失败**：
```
[认证] 认证失败：Student not found or inactive
[提示] 请联系监考老师确认您的账号已在系统中注册
```

**网络错误**：
```
[网络] 连接失败：无法连接到服务器 http://localhost:3000: ...
[提示] 请确认服务器地址是否正确，以及网络是否畅通
```

**成绩上传失败**：
```
[错误] 成绩上传失败，请联系监考老师手动记录成绩
       本地成绩：85 / 100
       成绩已备份至：/home/student/.exam_agent/logs/score_backup_1234567890.json
```

## 本地备份

如果成绩上传失败，客户端会自动备份到：

```
~/.exam_agent/logs/score_backup_<timestamp>.json
```

备份内容包含：
- 考试记录 ID
- 每道题得分
- 总分
- 评分详情

## 常见问题

### Q1: 如何切换服务器地址？

```bash
# 方式 1：命令行指定
python3 exam_agent.py --server http://新服务器 IP:3000

# 方式 2：配置模式
python3 exam_agent.py --config
# 输入新地址

# 方式 3：直接修改配置文件
nano ~/.exam_agent/config.json
```

### Q2: 自动模式太快，来不及查看题目？

使用手动确认模式：

```bash
python3 exam_agent.py --server http://服务器 IP:3000
# 显示题目后，按 Enter 键才开始评分
```

### Q3: 评分脚本执行失败？

检查日志：
```bash
cat ~/.exam_agent/logs/agent_*.log
```

常见原因：
- 数据库服务未启动
- 评分脚本路径错误
- 权限不足

### Q4: 设备绑定错误？

```bash
# 清除设备绑定
rm ~/.exam_agent/token.json

# 重新运行
python3 exam_agent.py --auto
```

### Q5: 考试中途退出怎么办？

重新运行即可，系统会恢复之前的考试记录：

```bash
python3 exam_agent.py --auto
```

## 评分脚本格式

客户端支持解析以下评分输出格式：

### score.sh 格式

```bash
***第 1 题数据库卸载得分***:4
***第 2 题重新安装部署数据库得分***:12
...
总得分：85
```

### 扣分格式

```bash
数据库软件未成功卸载:-4
数据库名:-1
实例名:-1
```

### JSON 格式

```json
{
  "totalScore": 85,
  "details": [
    {"description": "数据库未安装", "deduction": 5, "passed": false}
  ]
}
```

## 技术规格

| 项目 | 说明 |
|------|------|
| Python 版本 | 3.8+ |
| 依赖库 | requests |
| Token 有效期 | 8 小时 |
| 等待超时 | 1 小时 |
| 评分超时 | 300 秒/题 |
| 上传重试 | 5 次 |

## 相关文件

| 文件 | 说明 |
|------|------|
| `client_agent/exam_agent.py` | 客户端主程序 |
| `~/.exam_agent/config.json` | 配置文件 |
| `~/.exam_agent/token.json` | 认证 Token |
| `~/.exam_agent/logs/` | 日志目录 |
| `~/.exam_agent/scripts/` | 临时评分脚本 |

## 更新日志

### v1.2 (2026-03-22)
- ✅ 新增自动评分模式 (`--auto`)
- ✅ 新增结束考试 API (`finishExam`)
- ✅ 优化评分输出解析（支持 score.sh 格式）
- ✅ 新增本地备份功能

### v1.1 (2026-03-20)
- ✅ 支持 score.sh 评分格式解析
- ✅ 新增题目得分提取
- ✅ 优化错误处理
