# install.sh 更新总结

## 新增功能

### 1. `--test-score` 参数

**功能**: 专门测试 score.sh 评分解析功能

**用法**:
```bash
bash install.sh --test-score
```

**输出示例**:
```
══════════════════════════════════════════
  测试 score.sh 评分解析功能
══════════════════════════════════════════
============================================================
测试 score.sh 输出解析
============================================================

[测试 1] score.sh 格式（扣分场景）
----------------------------------------
总分：37
题目得分：{1: 0, 2: 6, 3: 5, 4: 2, 5: 4, 6: 8, 7: 4, 8: 3, 9: 5}
扣分项数量：20
原始输出长度：575
✓ 测试通过

[测试 2] 完美成绩（100 分）
----------------------------------------
总分：100
题目得分：{1: 4, 2: 14, 3: 8, 4: 18, 5: 8, 6: 20, 7: 8, 8: 10, 9: 10}
扣分项数量：0
✓ 测试通过

[测试 3] JSON 格式
----------------------------------------
总分：85
详情：[{'description': '数据库未安装', 'deduction': 5, 'passed': False}]
✓ 测试通过

[测试 4] 空输出
----------------------------------------
总分：0
题目得分：{}
✓ 测试通过

============================================================
所有测试通过！
============================================================
```

### 2. `--demo-exam` 增强

**新增功能**: 自动导入 score.sh 评分规则配置

**流程**:
1. 导入 score.sh 评分规则配置（9 道数据库题目）
2. 创建管理员账号
3. 创建学生账号
4. 创建考试题目
5. 创建考试场次
6. 启动考试
7. 学生端参加考试
8. 执行 score.sh 评分
9. 提交成绩
10. 查看成绩报表

**用法**:
```bash
bash install.sh --demo-exam
```

### 3. `--test-only` 增强

**新增测试项**: [测试 0] score.sh 评分解析测试

现在 `--test-only` 会首先验证 score.sh 评分解析功能是否正常。

**用法**:
```bash
bash install.sh --test-only
```

## 修改的文件

| 文件 | 修改内容 |
|------|---------|
| `install.sh` | 添加 `--test-score` 参数、增强 `--demo-exam`、增强 `--test-only` |
| `client_agent/exam_agent.py` | 更新 `_parse_output()` 支持 score.sh 格式 |
| `test_score_parser.py` | 新增测试脚本 |
| `import_score_rules.py` | 新增评分规则导入脚本 |
| `SCORE_INTEGRATION.md` | 新增集成文档 |

## 测试用例

### score.sh 输出格式支持

测试脚本验证以下格式的解析：

1. **题目得分格式**:
   ```
   ***第 1 题数据库卸载得分***:4
   ***第 2 题重新安装部署数据库得分***:14
   ```

2. **总得分格式**:
   ```
   总得分：100
   总得分:85
   ```

3. **扣分信息格式**:
   ```
   数据库软件未成功卸载:-4
   数据库名:-1
   实例名:-1
   ```

4. **JSON 格式** (兼容其他评分脚本):
   ```json
   {"totalScore": 85, "details": [...]}
   ```

## 使用场景

### 场景 1: 开发测试

```bash
# 快速验证 score.sh 解析功能
bash install.sh --test-score
```

### 场景 2: 完整考试演示

```bash
# 一键演示完整考试流程（含 score.sh 评分）
bash install.sh --demo-exam
```

### 场景 3: 系统验证

```bash
# 运行所有测试（包含 score.sh 解析测试）
bash install.sh --test-only
```

## 帮助信息更新

```
测试模式（完整考试流程演示）：
  --demo-exam        一键测试：导入评分规则 + 创建数据 + 学生答题 + score.sh 评分
  --test-score       仅测试 score.sh 评分解析功能

示例：
  bash install.sh --test-score            # 测试 score.sh 评分解析
  bash install.sh --demo-exam             # 演示考试全流程测试（含 score.sh）
```

## 依赖关系

```
install.sh
├── test_score_parser.py (新增)
│   └── client_agent/exam_agent.py (ScriptExecutor._parse_output)
├── import_score_rules.py (新增)
│   └── 数据库连接
└── demo_exam_test.py (增强)
    └── import_score_rules.py
```

## 测试覆盖率

| 测试项 | 状态 |
|--------|------|
| score.sh 扣分场景解析 | ✓ |
| score.sh 完美成绩解析 | ✓ |
| JSON 格式解析 | ✓ |
| 空输出处理 | ✓ |
| install.sh --test-score | ✓ |
| install.sh --test-only | ✓ |
| install.sh --demo-exam | ✓ |

## 后续优化建议

1. **添加更多测试用例**: 覆盖更多 score.sh 变体格式
2. **性能测试**: 大量题目时的解析性能
3. **错误处理**: 异常输出格式的容错能力
4. **日志记录**: 详细的解析日志便于调试
