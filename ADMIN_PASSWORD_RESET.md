# 管理员密码重置功能

## 功能说明

`install.sh --reset-db` 功能已修改为**管理员密码重置工具**，不再重置整个数据库。

## 功能特点

- ✅ **保留所有数据**：不会删除考试记录、学生信息、题目等数据
- ✅ **重置管理员密码**：为所有管理员账号生成新的随机密码
- ✅ **自动创建管理员**：如果没有管理员账号，会自动创建一个默认的
- ✅ **安全可靠**：使用强密码哈希算法，密码格式为 `Admin` + 8位随机字符

## 使用方法

```bash
# 重置管理员密码（保留所有数据）
sudo bash install.sh --reset-db
```

## 操作流程

1. 运行命令后会显示确认提示
2. 输入 `yes` 确认操作
3. 脚本会连接数据库并重置密码
4. 显示新的管理员账号和密码

## 示例输出

```
🔑 管理员密码重置工具

  此操作将：
    1. 重置所有管理员账号的密码
    2. 创建新的默认管理员账号（如果不存在）
    3. 保留所有考试数据和学生信息

  确定要重置管理员密码吗？(输入 yes 确认): yes

========================================
  管理员账号重置成功！
  用户名：admin
  新密码：Adminlji7y77q
  请登录后及时修改密码！
========================================
```

## 重要提醒

- 新密码格式为 `Admin` + 8位随机字符
- 请登录后及时修改密码
- 所有现有管理员的密码都会被重置为相同的新密码
- 此操作不会影响学生账号和其他数据

## 与之前的区别

| 旧版本 | 新版本 |
|--------|--------|
| 删除整个数据库 | 仅重置管理员密码 |
| 删除所有数据 | 保留所有考试数据 |
| 需要重新初始化 | 快速重置密码 |
| 高风险操作 | 安全操作 |
