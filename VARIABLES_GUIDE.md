# 题目变量系统规范文档

## 1. 概述

本文档详细描述了Linux考试系统中题目变量系统的设计、实现和使用方法。该系统允许考试管理员创建动态题目，根据不同的题目套数自动替换题目中的变量，并生成相应的评分脚本。

## 2. 变量命名规则

### 2.1 基本命名规则

- 变量采用双大括号包围的形式：`{{variable_name}}`
- 变量名只能包含字母、数字和下划线
- 变量名区分大小写
- 变量名应具有描述性，便于理解其用途

### 2.2 系统内置变量

| 变量名 | 描述 | 示例值 |
|--------|------|--------|
| `{{username}}` | 当前学生的用户名 | `student123` |

### 2.3 题目套数变量

题目套数变量用于区分不同的题目版本，格式为：

- `{{a}}` - a套题的主变量
- `{{b}}` - b套题的主变量
- `{{c}}` - c套题的主变量

### 2.4 题目特定变量

题目特定变量用于在同一套题中区分不同的题目，格式为：

- `{{a1}}` - a套题第1题的变量
- `{{a2}}` - a套题第2题的变量
- `{{b1}}` - b套题第1题的变量

### 2.5 动态内容变量

根据不同题目套数自动替换的动态内容变量：

| 变量名 | 描述 | a套题值 | b套题值 |
|--------|------|---------|---------|
| `{{a_dbname}}` | 数据库名称 | `DAMENG` | `DMEXAM` |
| `{{a_instance}}` | 实例名称 | `PROD` | `TEST` |
| `{{a_port}}` | 端口号 | `5236` | `5237` |
| `{{b_dbname}}` | 数据库名称 | `DAMENG` | `DMEXAM` |
| `{{b_instance}}` | 实例名称 | `PROD` | `TEST` |
| `{{b_port}}` | 端口号 | `5236` | `5237` |

## 3. 自动出题流程

### 3.1 题目抽取

1. 考试系统根据考试配置（题目数量、难度、分类等）从题库中随机抽取题目
2. 系统为每个考试会话分配一个题目套数（如a套题、b套题等）
3. 系统为每个题目生成相应的变量上下文

### 3.2 变量替换

1. 系统根据题目套数和题目索引生成变量上下文
2. 系统将题目内容中的变量占位符替换为实际值
3. 个性化后的题目内容存储在数据库中，供学生查看和作答

### 3.3 评分脚本生成

1. 系统根据题目套数选择相应的评分规则
2. 系统将评分脚本中的变量替换为实际值
3. 生成的评分脚本用于学生作答后的自动评分

## 4. 变量使用示例

### 4.1 题目内容示例

```
请在用户 {{username}} 的机器上完成以下操作：

1. 卸载现有的达梦数据库软件
2. 重新安装达梦数据库，要求：
   - 数据库名：{{a_dbname}}
   - 实例名：{{a_instance}}
   - 端口号：{{a_port}}
3. 验证数据库服务是否正常启动
```

### 4.2 评分脚本示例

```bash
#!/bin/bash
# 评分脚本 - 适用于a套题

# 检查数据库名是否正确
dbname=$(head -1 /home/dmdba/dbname.buf | tr -s " " | cut -d " " -f 2)
if [ $dbname = "{{a_dbname}}" ]; then
    echo "数据库名正确"
else
    echo "数据库名错误:-1"
    score=$((score-1))
fi
```

## 5. 配置和管理

### 5.1 题目管理

- 在题目管理界面创建题目时，可以直接在题目内容中使用变量占位符
- 系统会自动识别并处理这些变量

### 5.2 考试配置

- 在创建考试时，可以指定题目数量、难度、分类等
- 系统会根据这些配置自动抽取题目并生成变量

### 5.3 评分规则管理

- 可以为不同的题目套数创建不同的评分规则
- 评分规则中可以使用变量，系统会自动替换

## 6. 错误处理

### 6.1 变量未定义

- 如果题目中使用了未定义的变量，系统会在日志中记录警告
- 未定义的变量会保持原样显示给学生

### 6.2 变量替换失败

- 如果变量替换过程中出现错误，系统会在日志中记录错误
- 系统会使用原始题目内容作为备选方案

## 7. 日志和监控

- 系统会记录所有变量替换操作
- 可以通过日志查看每个学生的题目变量替换情况
- 可以监控变量替换的成功率和错误率

## 8. 扩展建议

### 8.1 支持更多题目套数

目前系统支持a、b两套题，可以扩展支持更多套题

### 8.2 支持自定义变量

可以允许管理员在后台定义自定义变量，增强系统的灵活性

### 8.3 支持动态变量值

可以实现根据学生信息或其他条件动态生成变量值的功能

## 9. 常见问题

### 9.1 如何添加新的题目套数？

1. 在数据库中添加新的题目套数配置
2. 更新`generateVariableContext`函数，添加新的变量映射
3. 创建相应的评分规则和脚本

### 9.2 如何修改现有变量的值？

修改`generateVariableContext`函数中的变量映射即可

### 9.3 如何测试变量替换功能？

可以使用系统提供的测试工具，或者创建测试题目和学生账号进行测试
